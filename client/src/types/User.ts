export interface User {
    _id?: string
    first_name: string
    last_name: string
    email: string
    password: string
    status: string
    occupation: string
    image: string
    skills: string[]
}